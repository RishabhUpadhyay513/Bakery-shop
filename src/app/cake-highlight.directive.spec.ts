import { CakeHighlightDirective } from './cake-highlight.directive';

describe('CakeHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new CakeHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
